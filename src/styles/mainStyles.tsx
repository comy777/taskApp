import {StyleSheet} from 'react-native';

export const mainStyles = StyleSheet.create({
  container: {
    padding: 15,
  },
  input: {
    height: 70,
    width: '100%',
    borderRadius: 15,
    borderWidth: 2,
    borderColor: 'teal',
    marginBottom: 15,
    paddingHorizontal: 15,
    fontSize: 18,
    color: 'teal',
  },
});
